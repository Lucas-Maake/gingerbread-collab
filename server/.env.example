# Server Configuration
PORT=3001
NODE_ENV=development

# Client URL for CORS
CLIENT_URL=http://localhost:3000

# Optional Redis for Socket.IO adapter and room snapshot persistence
REDIS_URL=redis://localhost:6379

# Optional snapshot settings
ROOM_SNAPSHOT_FILE=./data/room-snapshots.json
ROOM_SNAPSHOT_TTL_MS=86400000
